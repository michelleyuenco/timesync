rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /groups/{groupId} {
      // MVP: open read/write — add auth rules later
      allow read, write: if true;
    }
  }
}
